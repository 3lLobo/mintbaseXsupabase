import { Button, } from "@chakra-ui/react";


export const SpinnerContainer = () => {
    return (
        <Button
            isLoading
            colorScheme='blue'
        >
        </Button>
    )
}

export default SpinnerContainer